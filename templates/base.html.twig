<!doctype html>
<html lang="fr" data-bs-theme="auto">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Florenza Création artisanale - 100% Made in France</title>
    <meta name="description" content="La boutique spécialisée dans la fabrication Made In France">
    
    {# commande asset retourne le chemin du public paths (dossier public)  qui va lui même contenir les diférents asset si je veux modifier les diférents asset j'aurais pas à le faire partout (regard doc) #}
    <link rel="stylesheet" href="{{ asset ('assets/css/style.css') }}">

    <link href="{{ asset ('assets/css/bootstrap.min.css') }}" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="{{ asset ('assets/css/carousel.css') }}" rel="stylesheet">
    <link href="{{ asset ('assets/css/custom.css') }}" rel="stylesheet">
    <script src="https://unpkg.com/feather-icons"></script>
</head>
<body>

<header data-bs-theme="dark">
    <nav class="navbar navbar-expand-md navbar-custom fixed-top">
    
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}">Florenza</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <ul class="navbar-nav me-auto mb-2 mb-md-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    {# allCategories dans cette variable, j'ai l'ensemble de mes objet categories de mon site internet  #}
                    {% for category in allCategories %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path ('app_category', { slug : category.slug}) }}">{{ category.name }}</a>
                        </li>
                    {% endfor %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Contact</a>
                    </li>
                </ul>
                <div class="speed-menu">
                    {#  #}
                    {% if app.user %}
                        <a href="{{ path('app_account') }}"><i data-feather="user"></i> {{ app.user.firstname }}</a>
                    {% else %}
                        <a href="{{ path('app_login') }}"><i data-feather="user"></i></a>
                    {% endif %}
                    
                    <a href="{{ path ('app_cart') }}">
                        <i data-feather="shopping-cart"></i>
                        <span class="badge text-bg-light">{{ fullCartQuantity }}</span>
                    </a>
                </div>
            </div>
        </div>
    </nav>
</header>

<main>
    {# je viens créer un container qui va afficher tous les massages sur mes différentes vues  #}
<div class="container">
    {# La j'ai une boucle for qui vient chercher tous les messages qui sont stock dans app.flashes(qui stock tous les message flashes) de dans on revient rebouclé sur les message et ensuite on affiche les messages. la class voir la doc bootstrap #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert mt-4 alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
</div>

    

{% block body %}{% endblock %}

    <!-- FOOTER -->
    <footer class="footer-custum">
        <div class="container ">
            <div class="row align-items-center">
                <!-- Colonne 1 : Navigation (à gauche) -->
                <div class="col-md-4 text-start">
                    <h5>Navigation</h5>
                    <ul class="list-unstyled">
                        <li><a href="#">Accueil</a></li>
                        <li><a href="#">Parfum</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </div>
    
                <!-- Colonne 2 : Conditions générales (au centre) -->
                <div class="col-md-4 text-center">
                    <h5>Informations légales</h5>
                    <ul class="list-unstyled d-inline-block">
                        <li><a href="#">Conditions Générales de Vente (CGV)</a></li>
                        <li><a href="#">Conditions Générales d'Utilisation (CGU)</a></li>
                        <li><a href="#">Politique de confidentialité</a></li>
                    </ul>
                </div>
    
                <!-- Colonne 3 : Contact (à droite) -->
                <div class="col-md-4 text-end">
                    <h5>Contact</h5>
                    <p>Adresse email :</p>
                    <p><a href="mailto:contact@florenza.com">contact@florenza.com</a></p>
                    <div class="social-icons">
                        <a href="#" aria-label="Facebook"><i data-feather="facebook"></i></a>
                        <a href="#" aria-label="Instagram"><i data-feather="instagram"></i></a>
                        <a href="#" aria-label="Twitter"><i data-feather="twitter"></i></a>
                    </div>
                </div>
            </div>
    
            <!-- Ligne de copyright -->
            <div class="row">
                <div class="col text-center">
                    <p class="mt-3">&copy; 2017–2024 Florenza. Tous droits réservés.</p>
                </div>
            </div>
        </div>
    </footer>
    

</main>

<script src="{{ asset ('assets/js/bootstrap.bundle.min.js') }}"></script>
<script src="{{ asset ('assets/js/global.js') }}"></script>
</body>
</html>

